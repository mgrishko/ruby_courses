<div id='left-side'>
  <div class="top-box">
  </div>
  <%= link_to t('item.add_item'), new_base_item_path, :class=>"add-product" if current_user.supplier? %>
  <%= render '/nav' %>
</div>
<%= render :partial => 'results' %>

