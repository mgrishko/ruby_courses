= title t('.title'), page_title: false

- content_for(:sidebar) do
  = create_link Product, class: "btn large primary"

%article
  %table
    %tbody
      - @products.each do |product|
        %tr
          %td= image_tag("product_small.png")
          %td
            %h2= product.show_link




