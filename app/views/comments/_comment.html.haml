%tr.comment{ id: comment.id }
  %td
    .links
      = comment.destroy_link
    -#%em= "#{comment.user.full_name}, #{comment.created_at.strftime('%d %b %Y, %H:%M')}"
    %em= comment.user.full_name
    = simple_format comment.body
