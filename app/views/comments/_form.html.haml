= simple_form_for @comment,
        url: send("#{commentable.class.name.underscore}_comments_path", commentable.id),
        remote: true do |f|
  = f.error_notification

  .inputs
    = f.input :body, as: :text, label: false

  .actions
    = f.button :submit, class: "primary small"
