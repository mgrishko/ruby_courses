<h1>Editing packaging_item</h1>

<%= form_for([@base_item, @packaging_item]) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Back', base_item_packaging_item_path(@base_item, @packaging_item) %>

