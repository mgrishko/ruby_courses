<div class="arrow"></div>

<% remote_form_for(@packaging_item, :url => base_item_packaging_item_path(@base_item, @packaging_item)) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to_remote 'Cancel', :url => base_item_packaging_item_path(@base_item, @packaging_item), :method => :get %>
