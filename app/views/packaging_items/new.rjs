pi_id = @packaging_item.parent ? "packaging_item_new_#{@packaging_item.parent.id}" : 'packaging_item_new'

page.replace_html pi_id, :partial => 'new'
page[pi_id].remove_class_name('new') if page[pi_id].has_class_name('new')
page[pi_id].scroll_to
