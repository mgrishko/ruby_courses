<h1>Listing packaging_items</h1>
<table>
  <tr>
  </tr>

<% for packaging_item in @packaging_items %>
  <tr>
    <td><%=h packaging_item.name %>
    <td><%= link_to 'Show', base_item_packaging_item_path(@base_item, packaging_item) %></td>
    <td><%= link_to_item_with_count 'List children', base_item_packaging_items_path(@base_item, packaging_item, :parent_id => packaging_item.id), packaging_item.children.count %></td>
    <td><%= link_to 'Edit', edit_base_item_packaging_item_path(@base_item, packaging_item) %></td>
    <td><%= link_to 'Destroy', [@base_item, packaging_item], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%=render :partial => "url_bar", :locals => {:base_item => @base_item, :disable_new => true} %>
<% if defined? @parent %>
  | <%= link_to 'Up', base_item_packaging_items_path(@base_item, :parent_id => @parent.parent ? @parent.parent.id : nil) %>
<% end %>
