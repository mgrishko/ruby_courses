%h1 Users

%table.grid
	- @memberships.each do |m|
		%tr
			%td= m.display_name
			%td= m.role_name
			%td= link_to "Edit", edit_membership_path(m)
			%td= button_to "Delete", m, :method => :delete, :class => "btn small danger"