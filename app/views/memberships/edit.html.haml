= title t(".title")

= simple_form_for @membership do |f|
  = f.error_notification
  .inputs
    = f.input :role, collection: MembershipDecorator.role_select_options,
      include_blank: false
  .actions
    = f.button :submit, class: 'primary'
    = link_to t("cancel"), :back
