- flash.each do |key, value|
  .alert-message.fade.in{ class: "#{key}", :"data-alert" => "#{key}" }
    %a.close{ href: "#" }×
    %p= value
    -#
    -#
    -#<div class="alert-message success fade in" data-alert="alert">
    -#  <a class="close" href="#">×</a>
    -#  <p><%= notice %></p>
    -#</div>