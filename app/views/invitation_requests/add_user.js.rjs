page[:add_user_wrapper].html(render :partial =>'form')
if @user.valid? 
  page[:form_wrapper].hide()
  page[:add_user_wrapper].prepend "Successfully added. Credentials sent to #{@user.email}"
else
  page[:form_wrapper].show()
end

