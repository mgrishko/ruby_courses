defaults: &defaults
  project_name: "GoodsMaster"
  app_subdomain: "app"
  send_mail_from: "system@GoodsMasterHQ.com"

development:
  <<: *defaults

test:
  <<: *defaults

cucumber:
  <<: *defaults

qa:
  <<: *defaults

staging:
  <<: *defaults

production:
  <<: *defaults